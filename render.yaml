services:
  - type: web
    name: speaking-photo
    env: node
    plan: free
    buildCommand: npm install && npm run setup:piper
    startCommand: npm start
    healthCheckPath: /
    autoDeploy: true
