{
  "name": "speaking-photo",
  "version": "2.0.0",
  "description": "Ask a question, get a Gemini answer, and watch your avatar speak it — Gemini TTS + Simli streaming lip-sync.",
  "private": true,
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "setup:simli-face": "node scripts/setup-simli-face.js",
    "build": "node scripts/build-client.js",
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "simli-client": "^3.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
